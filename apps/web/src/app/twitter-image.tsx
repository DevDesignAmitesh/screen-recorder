// Twitter card image — reuses the same generated image as opengraph-image.tsx
// (see that file for the actual design) so both cards stay in sync.
export { alt, contentType, default, size } from "./opengraph-image";
